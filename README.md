# David Slings

Handcrafted slings inspired by the weapon that changed history. Single-product ecommerce store.

**Stack:** Vite 7 + React 19 + TypeScript + Tailwind CSS 4 + Supabase + Stripe + Cloudflare Pages

## Development

```bash
npm install
npm run dev          # Dev server at localhost:5173
npm run build        # Production build
npm run preview      # Preview production build
```

## Environment Variables

Copy `.env.local` and fill in your keys:

```
VITE_SUPABASE_URL=https://sxsmcywsfhxosnkmocrx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_STRIPE_PUBLISHABLE_KEY=your-stripe-pk
```

## Setup Guide

See below for full setup of Supabase, Stripe, Cloudflare, and GoDaddy DNS.
